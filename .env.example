# Leave both values blank to run the deterministic mock demo.

# Gemini — required for live ADK mode; restrict to Generative Language API.
GOOGLE_API_KEY=

# Maps — required for live ADK mode; restrict to Directions API.
GOOGLE_MAPS_API_KEY=
